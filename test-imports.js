// Archivo de prueba para verificar que los imports funcionan
console.log('🔍 Test de imports iniciado...');

try {
  console.log('✅ App cargado correctamente');
} catch (e) {
  console.error('❌ Error:', e);
}
